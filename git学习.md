```
    // 添加到本地仓库
    git add .  

    // git 提交信息
    git commit -m "[你的提交信息]"

    // git 上传
    git push

    // 查看状态
    git status

    // 新建分支并切换到分支处
    git checkout -b [分支名称]

    // 切换分支
    git checkout [分支名称]

    // 查看分支
    git branch

    // 创建分支
    git branch [分支名称]
```